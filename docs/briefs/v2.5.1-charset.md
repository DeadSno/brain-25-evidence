# BRIEF v2.5.1 — СОЗДАНИЕ serve.py + CHARSET-ГАРДЫ

## ЗАДАЧИ

### 1. Создать docs/serve.py (новый файл, код ниже)
```python
"""Локальный сервер docs/ с UTF-8 заголовками (лечит кракозябры)."""
import os
import sys
from http.server import HTTPServer, SimpleHTTPRequestHandler
from pathlib import Path

DOCS = Path(__file__).resolve().parent
PORT = int(sys.argv[1]) if len(sys.argv) > 1 else 8000


class UTF8Handler(SimpleHTTPRequestHandler):
    def __init__(self, *args, **kwargs):
        super().__init__(*args, directory=str(DOCS), **kwargs)

    def end_headers(self):
        path = self.translate_path(self.path)
        if path.endswith(".html"):
            self.send_header("Content-Type", "text/html; charset=utf-8")
        elif path.endswith(".js"):
            self.send_header("Content-Type", "application/javascript; charset=utf-8")
        elif path.endswith(".css"):
            self.send_header("Content-Type", "text/css; charset=utf-8")
        elif path.endswith(".json"):
            self.send_header("Content-Type", "application/json; charset=utf-8")
        super().end_headers()


if __name__ == "__main__":
    os.chdir(DOCS)
    server = HTTPServer(("localhost", PORT), UTF8Handler)
    print(f"Сервер: http://localhost:{PORT}/ (docs/)")
    print("Стоп: Ctrl+C")
    try:
        server.serve_forever()
    except KeyboardInterrupt:
        print("\nСервер остановлен")
        server.server_close()
```

### 2. Во ВСЕХ docs/*.html проверить/добавить <meta charset="utf-8">
ПЕРВОЙ строкой внутри <head>:
```html
<!DOCTYPE html>
<html lang="ru">
<head>
<meta charset="utf-8">
<!-- остальное -->
```
Проверить: index.html, map.html, faq.html, glossary.html, methodology.html,
changelog_public.html, style.css (не нужен, но проверить отсутствие BOM).

### 3. Тесты tests/test_html_charset.py
```python
"""Гард: meta charset=utf-8 в первых 512 байтах каждого docs/*.html."""
from pathlib import Path

DOCS = Path(__file__).resolve().parents[1] / "docs"


def test_all_html_have_charset():
    html_files = list(DOCS.glob("*.html"))
    assert len(html_files) > 0, "нет HTML-файлов в docs/"
    
    for html_file in html_files:
        content = html_file.read_bytes()[:512]
        assert b'<meta charset="utf-8">' in content or b"<meta charset='utf-8'>" in content, \
            f"{html_file.name}: отсутствует <meta charset='utf-8'> в первых 512 байтах"


def test_serve_py_exists():
    serve = Path(__file__).resolve().parents[1] / "docs" / "serve.py"
    assert serve.exists(), "docs/serve.py не создан"
```

### 4. e2e_smoke.py: добавить проверку статической строки
```python
# После проверки данных:
assert "Как мы проверяем" in pg.content() or "Открытые данные" in pg.content(), \
    "Статическая строка UTF-8 не рендерится (кракозябры?)"
```

### 1.5 Перенести блок Yandex.Metrika из <head> в начало <body>
Порядок в <head> после правки: charset → viewport → title → og-* →
canonical → стили. Метрика-сниппет официально работает и сразу после
<body> — переносом мы гарантируем charset в первых 1024 байтах
даже с учётом инъекций локального антивируса в HTTP-поток.

### 1.6 Гард-тест усилить: не «charset в первых 512 байтах»,
а «charset — ПЕРВЫЙ тег внутри <head>» (regex: <head>\s*<meta charset)
для всех docs/*.html, иначе FAIL.

### 5. docs/methodology.html: кейс №5 в «Ошибки и исправления»
```
5. Локальные кракозябры-2 (сентябрь 2026):
   Файл docs/serve.py отсутствовал, простой http.server не шлёт charset,
   meta charset стояла не первой строкой → браузер в русской локали
   угадывал CP1251, данные из fetch (JSON) читались нормально,
   статические строки — каша. Прод не пострадал (GitHub Pages шлёт charset).
   Починено: создан docs/serve.py с заголовками, meta charset первой строкой,
   гард-тест на все HTML, статическая строка в e2e.
```

### 6. README.md: добавить инструкцию локального запуска
```markdown
## Локальный запуск
```bash
python docs/serve.py
# → http://localhost:8000/
```
```

## ПРИЁМКА
- docs/serve.py создан и запускается без ошибок
- `python docs/serve.py` → http://localhost:8000/ → UTF-8 в заголовках
- Все docs/*.html имеют <meta charset="utf-8"> первой строкой
- test_html_charset.py зелёный
- e2e_smoke проверяет статическую строку
- methodology.html содержит кейс №5
- README содержит инструкцию запуска
- pytest зелёный
- тег v2.5.1

## КОММИТЫ
- feat: docs/serve.py with UTF-8 headers
- fix: meta charset first in all HTML
- test: charset guard + serve.py exists
- test: e2e static string check
- docs: methodology case #5 + README local server

## ДОКЛАД
хэши, вывод `python docs/serve.py` (первые 3 строки),
вывод pytest, строка «жди бриф v2.6»