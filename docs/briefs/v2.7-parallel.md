# BRIEF v2.7 — ПАРАЛЛЕЛЬНЫЙ РЕЛИЗ (3 исполнителя, P8-P12)

## СОХРАНИТЬ КАК docs/briefs/v2.7-parallel.md
## ЗАПУСКАТЬ ТОЛЬКО ПОСЛЕ ТЕГА v2.6.1

## АРХИТЕКТУРА ПАРАЛЛЕЛИЗМА
Три независимых контура, три ветки, изоляция кода по P12:
каждая часть живёт в СВОИХ файлах (tracker.js / pwa.js+sw.js / science2.js),
index.html получает от каждой части ровно одну строку подключения
в конце body (script) или в head (link/meta) — конфликты сведены к строкам.
data.json НЕ меняет никто (части читают, не пишут).

### ЧАСТЬ A · ТРЕКЕР ПРИЁМА (ветка v2.7-dev-a)
A1. localStorage-схема myCourse: {id: {start: ISO, days: int, taken: [ISO],
    note: str}}; битые значения → сброс с console.warn (гард как у favs)
A2. Кнопка «📅 В мой курс» в модалке: доза из dosage, длительность 30 дней
    (редактируемая); приватность-пометка: «данные хранятся только в вашем
    браузере, никуда не отправляются»
A3. Секция «Мои добавки» на главной: карточки курса с прогресс-баром
    (taken/days), кнопки «+ принял», «завершить», «убрать»
A4. Файлы: docs/tracker.js + одна строка script в index.html
A5. Тесты: schema-гард localStorage; e2e: добавить курс → прогресс 1/30
Отчёт: docs/reports/v2.7-part-a.md

### ЧАСТЬ B · PWA (ветка v2.7-dev-b)
B1. manifest.webmanifest: name/short_name/start_url/display=standalone/
    theme_color/background_color + иконки 192 и 512 PNG
B2. Иконки генерировать scripts/make_icons.py через playwright:
    скриншот мини-HTML с логотипом «🧠⚖️» на фирменном фоне нужного размера
    (не рисовать руками, не тянуть PIL)
B3. sw.js: cache-first для статики (html/css/js/иконки), network-first
    для data.json с fallback в кэш + бейдж «офлайн-режим» при отдаче из кэша
B4. Регистрация SW в index.html и map.html (одна строка каждая),
    link manifest + meta theme-color в head
B5. Тесты: manifest валидный JSON с обязательными полями; e2e: после
    reload navigator.serviceWorker.controller !== null
B6. Файлы: docs/manifest.webmanifest, docs/sw.js, docs/pwa.js,
    scripts/make_icons.py, icons в docs/icons/
Отчёт: docs/reports/v2.7-part-b.md

### ЧАСТЬ C · ПУЛЬС НАУКИ + СПАРКЛАЙНЫ (ветка v2.7-dev-c)
C1. scripts/build_ma_timeline.py: esearch retmax=100 по каждой добавке
    (запрос как в scienceIndex) + esummary пачками по 50 id → годы МА;
    кэш data/processed/ma_years.json; сон 0.4 c; один прогон, коммит
C2. docs/data_ma_timeline.json: агрегат по годам 2015-2026 (суммарно
    и топ-5 добавок по приросту)
C3. График «Пульс науки: мета-анализы по годам» на главной (Chart.js line)
C4. Спарклайн цены в модалке (блок «Экономика»): линия за 90 дней из
    data/processed/price_history.csv; при <2 точек — текст
    «история копится с v2.1, спарклайн появится после N замеров»
C5. Файлы: docs/science2.js + build-скрипт + одна строка script
C6. Тесты: схема timeline-JSON (годы монотонны, суммы ≥0); e2e: график
    рендерит >0 точек; спарклайн показывает текст-фолбэк при пустой истории
Отчёт: docs/reports/v2.7-part-c.md

## РАБОТА ОРКЕСТРАТОРА
O1. Три исполнителя параллельно (P1, P6), промпты по P2 с путями частей
O2. Мердж ПОСЛЕДОВАТЕЛЬНО: a → b → c; конфликты только в строках
    подключения index.html — разрешать сохранением всех трёх строк
O3. Неразрешимый конфликт или красный тест у части → стоп и доклад владельцу
O4. После всех мерджей: P4 — pytest -q и e2e_smoke самому, сырой вывод в доклад
O5. CHANGELOG [v2.7] + пост docs/drafts/post_v2.7.md + сводный отчёт
    docs/reports/v2.7.md (мердж-лог + хэши трёх частей + сырой pytest/e2e)
O6. Тег v2.7, push

## ПРИЁМКА
трекер: курс добавляется, прогресс растёт, localStorage-гард работает ·
PWA: manifest+SW зарегистрирован, офлайн-фолбэк data.json ·
пульс науки: линия 2015-2026 с >0 точек · спарклайн или честный фолбэк ·
pytest 78+ · e2e+ui-verify OK · тег v2.7

## ДОКЛАД
путь сводного отчёта + топ-5 по каждой части + сырой pytest/e2e +
строка «жду бриф v2.8»