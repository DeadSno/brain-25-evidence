# BRIEF v2.6.2 — ПЯТЬ UI-ФИКСОВ (encoding-хирургия + фильтры + дизайн)

## ПРИОРИТЕТ: ПЕРЕД v2.7. Если исполнители v2.7 уже запущены — СТОП,
сначала этот хотфикс в main, затем v2.7 перезапускается от нового main.

## ЗАДАЧИ

### F1. Encoding-хирургия script.js (и всех docs/*.js)
F1.1 Скрипт-фикс (построчно, с фолбэком):
     for line in text.splitlines():
         try: fixed_line = line.encode('cp1251').decode('utf-8')
         except (UnicodeEncodeError, UnicodeDecodeError): fixed_line = line
     Применить к docs/script.js и любому docs/*.js, где найдена каша.
     После фикса визуально сверить 5 строк: «Способность», «Цена ₽/мес»,
     «Показатель», «Сравнить», «Радар профиля».
F1.2 Гард-тест test_no_mojibake_in_js: в docs/*.js и docs/*.html отсутствуют
     биграммы 'РЎ', 'Рµ', 'Р°', 'в,S/' (каша-маркеры), иначе FAIL
F1.3 e2e: статическая строка ИЗ JS: assert «Показатель» in pg.content()
     (закрывает слепую зону: прошлая e2e-строка была из HTML)

### F2. График уважает фильтры
F2.1 Любое изменение фильтров (поиск/вердикт/категория/сортировка/избранное)
     перерисовывает пузырьковый чарт и квадрант по ОТФИЛЬТРОВАННОМУ списку
F2.2 Подпись под чартом: «показано N из 81 добавок (фильтр: …)»
F2.3 Тест: e2e ставит категорию «Спорт» → пузырей = числу спортивных добавок

### F3. Контраст ссылок на тёмной теме
F3.1 CSS: цвет ссылок из переменной темы (тёмная: #8ecbff + underline;
     светлая: текущий синий); проверить на .trustbar, methodology-ссылке,
     футере, модалке
F3.2 ui-verify: скриншоты ОБЕИХ тем, контраст ссылок ≥ 4.5:1

### F4. Радар профиля: нормализация осей
F4.1 Каждая ось нормируется в 0-100 (перцентиль внутри метрики по базе),
     подпись осей: «нормировано по базе»
F4.2 Тултип на оси: сырое значение добавки по метрике
F4.3 Тест: значения радара ∈ [0,100]

### F5. Компактные блоки доверия
F5.1 «Сначала ответим на 3 вопроса» → три карточки в ОДНУ строку (grid),
     по 2-3 строки текста, ссылка «подробнее в FAQ»
F5.2 «Как мы проверяем данные» → горизонтальная полоса из 5 чипов-иконок
     (PubMed ↗ · ручная вычитка · ночной бот · 81 тест · код ↗),
     каждый чип = ссылка; вертикальный список убрать
F5.3 На 375px карточки складываются в столбец, чипы переносятся
F5.4 e2e: высота секции доверия ≤ 40% прежней (замер boundingBox)

## ПРИЁМКА
каша отсутствует во всех UI-строках (гард + e2e JS-строка) ·
фильтры двигают график · ссылки читаемы в обеих темах ·
радар нормирован · блоки доверия компактные ·
pytest 84+ · e2e+ui-verify OK · тег v2.6.2

## ДОКЛАД
docs/reports/v2.6.2.md: хэши, до/после скриншоты секции доверия и радара,
сырой pytest/e2e (P4), строка «можно перезапускать v2.7»