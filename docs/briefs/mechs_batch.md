# BRIEF: Расширение mechs — батч

## Контекст

Проект brain-25-evidence: сайт про БАДы, 81 карточка в `docs/data.json`.
Каждая карточка содержит поле `mechs` — список механизмов действия.

**Сейчас у всех 81 карточки ровно 1 механизм.** Формат элемента:
["детальный механизм", "краткий эффект", "сила"]

Пример (Креатин):

["Фосфокреатиновый буфер: ресинтез АТФ в нейронах и мышцах", "Рабочая память под нагрузкой, сила", "сильно"]


**Цель:** расширить до 2-4 механизмов на карточку, не выдумывая — только из abstracts МА.

## Задача (батч)

Пользователь даёт в чате список из **5 карточек**. Для каждой:
- расширить `mechs` с 1 до 2-4 элементов
- источники: `reports/evidence/<id>.md` + текущая карточка в `docs/data.json`
- первый механизм оставить из `data.json`
- не выдумывать, только из abstracts
- формат `["механизм", "эффект", "сила"]`

### Правила

- **Не выдумывать.** Каждый новый механизм должен подтверждаться абстрактом из evidence-файла или очевидной физиологией, упомянутой там.
- **Формат элемента:** `["механизм (20-250 симв)", "эффект (5-120 симв)", "сила"]`
- **Сила — только из:** `"сильно"`, `"умеренно"`, `"слабо"`, `"контекст"`, `"маркетинг"`
- **Механизм** — молекулярный/физиологический уровень (что происходит в клетке/органе)
- **Эффект** — что чувствует/получает человек (кратко, через запятую)
- **Стиль** — как в существующих mech: короткие ёмкие формулировки, без воды
- **Первый механизм оставить как есть** (уже утверждён), новые добавлять после него
- **2-4 механизма всего.** Не больше 4.

## Что записать

Один файл: `reports/mechs_batch_1_proposal.json` (номер батча — в имени).

Формат:
{
  "B12": [...],
  "Омега-3": [...],
  ...
}

**Ничего не писать в `docs/data.json`.**

## Отчёт

`reports/mechs_batch_1.md` — обоснование каждого нового механизма со ссылками на PMID.

## DoD

- `reports/mechs_batch_1_proposal.json` создан, валидный JSON
- `reports/mechs_batch_1.md` создан, есть ссылки на PMIDs
- `docs/data.json` НЕ тронут
- Никаких коммитов (пользователь сам закоммитит после ревью)

## НЕ делать

- Не менять другие карточки
- Не править `docs/data.json`
- Не править `map.html` или `script.js`
- Не коммитить