# BRIEF v2.4 — КВАДРАНТ ДОКАЗАТЕЛЬНОСТИ + HOTFIX ОПЕЧАТОК

## ШАГ 0 (hotfix, тег v2.3.2, до ветки v2.4-dev)
1. src/content.py: «цитrus» → «цитрусовые», «Полиплофенол» → «Полифенол»
2. scripts/build_edu.py перегнать + снапшот
3. Guard-тест tests/test_content_latin.py: латинские буквы ВНУТРИ
   кириллических слов во всех текстовых полях data.json и content.py = FAIL
   (исключения: латиница в именах собственных/терминах типа EPA, DHA, NF-κB,
   TOTOX, LGG, Creapure — белый список в тесте)
4. pytest → мердж в main → тег v2.3.2 → push
Коммит отдельный: fix: typos in EDU_TOP10 + latin-in-cyrillic guard

## КОНТЕКСТ v2.4
Миссия: эффект, а не счётчик. scienceIndex = объём науки, Hedges' g = сила.
Квадрант из двух осей + грейды A-D + ключевые источники у вердиктов.
g попадает в data.json ТОЛЬКО верифицированным (approve-очередь).

## ЗАДАЧИ
1. Схема data.json (nullable): hedges_g, hedges_g_ci:[lo,hi], effect_outcome,
   grade, key_sources:[{citation, pmid, doi, year, g, ci}]
2. Тесты provenance:
   • g≠null ⇒ key_sources непустой, у каждого pmid или doi
   • -3 ≤ g ≤ 3; ci_lo < ci_hi
   • grade пересчитывается из (g, scienceIndex) функцией grade_of()
3. scripts/extract_g.py (код ниже): кандидаты g из абстрактов PubMed
4. docs/approve_queue.md: таблица кандидатов
   (добавка | pmid | год | g | CI | исход | сниппет | приор ±0.1 совпал?)
   В data.json НЕ коммитить без пометки владельца «ок» в очереди
5. PRIOR_G (код ниже) — приоры соавтора из памяти, ТОЛЬКО для сверки
6. UI: переключатель вкладок графика «Цена vs наука | Квадрант доказательности»;
   квадрант = Chart.js scatter: X=hedges_g (-0.2..0.6), Y=scienceIndex (log),
   r=metaCount, цвет=вердикт; добавок без g нет на квадранте →
   подпись «N добавок ждут верификации эффекта»; грейды A-D бейджем в карточке
   и в модалке (A «крепко доказано», B «умеренно», C «изучено много, эффект мал»,
   D «данных мало»)
7. docs/methodology.html: методология счёта + грейдов + раздел
   «Ошибки и исправления» (4 кейса текстами ниже); ссылка в футере index и map
8. CHANGELOG [v2.4] + черновик поста docs/drafts/post_v2.4.md
9. pytest + e2e (квадрант рендерит, грейды в DOM) + ui-verify → мердж → тег v2.4

## КОД

### scripts/extract_g.py
```python
"""Кандидаты Hedges' g из абстрактов PubMed (данные цикла 4)."""
import re
import requests

ESEARCH = "https://eutils.ncbi.nlm.nih.gov/entrez/eutils/esearch.fcgi"
EFETCH = "https://eutils.ncbi.nlm.nih.gov/entrez/eutils/efetch.fcgi"
G_RE = re.compile(r"(?:SMD|Hedges['’]?s?\s*g|Cohen['’]?s?\s*d|standardized mean "
                  r"difference)\s*(?:=|:)?\s*(-?\d+\.\d+)", re.I)
CI_RE = re.compile(r"95%\s*CI[^0-9-]*(-?\d+\.\d+)\s*(?:to|–|-|,)\s*(-?\d+\.\d+)", re.I)


def ma_pmids(query: str, n: int = 5) -> list[str]:
    r = requests.get(ESEARCH, params={"db": "pubmed", "retmode": "json", "retmax": n,
                                      "term": f"({query}) AND meta-analysis[pt]"}, timeout=30)
    r.raise_for_status()
    return r.json()["esearchresult"].get("idlist", [])


def abstract(pmid: str) -> str:
    r = requests.get(EFETCH, params={"db": "pubmed", "id": pmid,
                                     "rettype": "abstract", "retmode": "xml"}, timeout=30)
    r.raise_for_status()
    return " ".join(re.findall(r"<AbstractText[^>]*>(.*?)</AbstractText>", r.text, re.S))


def candidates(sid: str, query: str) -> list[dict]:
    out = []
    for pmid in ma_pmids(query):
        text = abstract(pmid)
        m = G_RE.search(text)
        if not m:
            continue
        ci = CI_RE.search(text)
        out.append({"id": sid, "pmid": pmid, "g": float(m.group(1)),
                    "ci": [float(ci.group(1)), float(ci.group(2))] if ci else None,
                    "snippet": text[max(0, m.start() - 80):m.end() + 80]})
    return out
```

### PRIOR_G (приоры соавтора из памяти; НЕ коммитить в data.json)
```python
PRIOR_G = {
    "Креатин": 0.36, "Кофеин": 0.42, "Мелатонин": 0.28, "Омега-3": 0.22,
    "Витамин D": 0.12, "Магний": 0.20, "Куркумин": 0.30, "Пробиотики": 0.25,
    "Цинк": 0.18, "Витамин C": 0.10, "Бакопа": 0.30, "Ашваганда": 0.35,
    "Родиола розовая": 0.25, "L-теанин": 0.20, "Гинкго билоба": 0.12,
    "Глюкозамин + хондроитин": 0.06, "Коллаген": 0.25, "Псиллиум": 0.30,
    "Берберин": 0.45, "Имбирь": 0.30,
}
# совпадение extract_g с приором ±0.1 = «приор совпал» в очереди
```

### grade_of()
```python
def grade_of(g, volume):
    if g is None:
        return None
    if volume < 50:
        return "D"
    if g >= 0.4 and volume >= 100:
        return "A"
    if g >= 0.2:
        return "B"
    return "C"
```

## ТЕКСТЫ «ОШИБКИ И ИСПРАВЛЕНИЯ» (дословно)
1. SAMe: поисковый токен матчил английское слово «same» и дал 7495 фантомных
   РКИ. Исправлено: запрос заменён на S-adenosylmethionine, добавлен тест запросов.
2. Кракозябры: локальный сервер отдавал UTF-8 без charset, браузер в русской
   локали подставлял CP1251. Исправлено: charset=utf-8 в заголовках + guard-тест.
3. Одна цифра без кавычек убила скрипт карты механизмов целиком. Исправлено:
   e2e-smoke проверяет обе страницы и консоль браузера.
4. Wildberries закрыл search-API TLS-фильтром: цены деградировали до статики
   карточек и честных null вместо выдуманных чисел. Исправление полного пути —
   в работе (reliability-трек).

## ПРИЁМКА
тег v2.3.2 (hotfix) · guard-тест латиницы зелёный · approve_queue.md с
кандидатами и колонкой «приор совпал» · квадрант рендерит (или честная
заглушка при пустых g) · грейды в DOM · methodology.html с 4 кейсами ·
pytest/e2e/ui-verify зелёные · тег v2.4

## ДОКЛАД
хэши, размер approve_queue, вывод pytest, строка «жду свайп очереди g»