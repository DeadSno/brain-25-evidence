# ROADMAP — brain-25-evidence

**Обновлено:** 2026-09-19
**Состояние:** 81 БАД, всё в проде, тесты зелёные.

---

## 📊 Текущее состояние

| Компонент | Статус |
|---|---|
| База | 81 карточка |
| Hedges' g | 63/81 (59 с CI) |
| Mech | **81/81**, среднее **3.07** (распределение: 2→7, 3→61, 4→13) |
| Radar профиля | 4 честные оси: Наука, База МА, Верифицированность, Полнота |
| Карта связей | ✅ с фильтром severity, сайдбаром, PNG-экспортом |
| Карта механизмов | ✅ гибрид: список + цепочки + оверлей + кросс-связи |
| Mobile | ❌ не оптимизирована |

**Sources:** PubMed (esearch/esummary/efetch), OpenAlex (citations), Wikipedia (views), ClinicalTrials.gov (ongoing).

---

## ✅ Сделано (сентябрь 2026)

### Данные
- **Cross-matched PMID устранён** (9a34195) — двухпроходный алгоритм в `fetch_hedges_g.py`, reused_shared: 0/81
- **7 карточек получили корректный g** — Креатин, B12, SAMe, Цинк, Кальций, Мака, Пикногенол
- **Хлорофилл**: терминология переписана, scienceIndex 323→7 (устранена ботаника)
- **Mech расширены** (16 батчей, 6f85539 + предыдущие): было 1.0 → стало 3.07 на карточку, ~250 новых механизмов, все с PMIDs из `reports/evidence/*.md`

### UI
- **Hedges' g на графике** — вкладка «Сила эффекта (g)» на `index.html`, ось X расширена до [-2.5, +2.5] (48d1d65)
- **Radar профиля** — 4 честные оси вместо reviews/trends (9a34195)
- **Карта механизмов** — цепочка + оверлей с кросс-связями по токенам (95d6381, 143b3e7)
- **Поддержка 2-4 mech** — один 💊 + N веток в цепочке, оверлей по индексу
- **Interactions** — размер нод по degree, сайдбар деталей, PNG-экспорт, фильтр severity (b98dd38 + предыдущие)

### Инфраструктура
- `scripts/apply_mechs.py` — безопасный патч mechs в data.json (валидация + бэкап + dry-run)
- `scripts/fetch_hedges_g.py` — двухпроходный сбор g
- `docs/briefs/mechs_batch.md` — шаблон брифа для батчей
- Snapshot-тесты: 114 passed

---

## 🔧 TODO мелкие (не блокеры)

### Из батчей mechs

1. **Фосфатидилсерин** — в `effects` заявлено «память у пожилых», evidence на самом деле про **СДВГ у детей** (МА 2021). Уточнить `who_needs` в data.json.

2. **Alpha-GPC** — вердикт `D` (не подтверждено) **спорен**: есть сигналы в комбинации с донепезилом (ADAS-Cog −13.9% за 12 нед, MD 3.50 сольного). Рассмотреть `C` (зависит от контекста).

3. **Хлорофилл** — «детокс» от афлатоксина **подтверждён** РКИ PNAS 2001 (180 чел., −55% аддуктов в моче, p=0.036). Вердикт `D` занижен — рассмотреть пересмотр.

4. **Клюква** — в `reports/evidence/Клюква.md` только ветеринарный обзор (собаки/кошки, 122 животных). Человеческих МА в evidence нет, хотя в `scienceIndex` их 23. Перепрогнать с расширенным `key_sources`.

### Из старых TODO

5. **URL карточек** — сейчас `#sup=ginkgo`, в Фазе 5 решено `?s=ginkgo`. Проверить, что работает на GitHub Pages, или сменить.

6. **citations + DOI** — Фаза 5 п.4: обогатить `key_sources` DOI через PubMed esummary (ArticleId IdType="doi"). Скрипт `scripts/enrich_dois.py`, ~234 запроса. `fetch_citations` потом использует DOI как fallback.

---

## 🚧 Бэклог крупный (по приоритету)

### 1. Атлас механизмов (4-6 ч)

Большой vis-network граф всех **81 × 3 = ~250 механизмов** с кросс-связями по пересечению слов. Логичное продолжение расширения mechs.

- `map.html`: вкладка «Атлас» рядом с текущей цепочкой
- Ноды: добавки (грейд), механизмы (кластеры по токенам), эффекты
- Сайдбар с фильтрами: категория, сила, грейд
- Клик по механизму → все добавки, которые его затрагивают

### 2. Q1.4 — adv-поля 61 карточки (долгая работа)

У 61 карточки нет `about`, `who_needs`, `onset`, `myths`, `food_sources`, `guidelines`, `how_to_choose` — только базовые поля (verdict, effects, dosage, course, caution). Ось «Полнота карточки» на радаре стоит на 40%.

- Гибрид: LLM-черновик по abstracts + ревью пользователя
- Батчи по 5 карточек, по шаблону Креатина
- Это оживит adv-поля и откроет путь к калькулятору

### 3. Калькулятор дозировок и взаимодействий (после Q1.4)

Идея от пользователя: пользователь добавляет свои БАДы + дозировки → сервис показывает:
- достаточность дозы (сравнение с `dosage` из карточки)
- конфликты между всеми парами
- синергии
- суммарная нагрузка по категориям

**Блокер:** Q1.4 — без `dosage`/`interactions` у 61 карточки калькулятор не построить.

### 4. Mobile оптимизация (2-3 ч)

Финальный слой — **после** всех новых фич, чтобы сделать один раз. Проверить: index.html, map.html, interactions.html, methodology, faq, glossary.

Известные проблемы:
- `#sidebar` в interactions.html (360px) перекрывает граф на телефоне 375px
- Верхняя панель index — 5 элементов не влезают
- Графики Chart.js — не адаптируются
- Цепочка mechs — слишком длинная по вертикали

### 5. Расширение базы (L)

Фаза 5 п.5: L > N. Сначала расширяем базу, потом обогащаем карту взаимодействий — новые карточки сразу попадают в карту.

---

## 📌 Решения Фазы 5 (2026-09-18) — актуальный статус

1. **Карта механизмов: гибрид** — ✅ реализовано (95d6381, 143b3e7)
2. **Группировка категорий: гибрид** — ✅ реализовано (69 категорий в map.html)
3. **URL карточек: `?s=`** — ⚠️ сейчас `#sup=`, см. TODO 5
4. **citations + DOI** — ⏳ не сделано, см. TODO 6
5. **L vs N: сначала L** — ⏳ не начато

---

## 🎯 Следующая сессия — старт

1. **ROADMAP + TODO** — этот файл
2. **Атлас механизмов** — граф 81×3
3. **Q1.4** — adv-поля 61 карточки
4. **Калькулятор** — после Q1.4
5. **Mobile** — финал